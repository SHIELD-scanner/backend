apiVersion: v1
kind: Secret
metadata:
  name: shield-backend-secret
  namespace: shield-system
type: Opaque
stringData:
  # MongoDB credentials - use stringData for plain text that will be base64 encoded
  mongodb-username: "ricardo"
  mongodb-password: "AdminExample"
  # Alternative: store the full URI as a secret (more secure)
  mongodb-uri: "mongodb://ricardo:AdminExample@mongo-1.shield.svc.cluster.local:32017/"
